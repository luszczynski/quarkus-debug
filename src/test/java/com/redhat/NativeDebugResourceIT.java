package com.redhat;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeDebugResourceIT extends DebugResourceTest {

    // Execute the same tests but in native mode.
}